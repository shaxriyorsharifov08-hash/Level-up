{
  "name": "level-up-hunter-system",
  "version": "1.0.0",
  "private": true,
  "description": "LEVEL UP — Hunter System. The app itself is a single index.html with no build step; this package exists only to run the tests.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
